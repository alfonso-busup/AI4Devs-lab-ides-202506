DB_PASSWORD=D1ymf8wyQEGthFR1E9xhCq
DB_USER=LTIdbUser
DB_NAME=LTIdb
DB_PORT=5432
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}"

# Local storage path for uploaded files (mounted volume in Docker)
STORAGE_LOCAL_PATH=./backend/uploads
# Max file size in MB
STORAGE_MAX_FILE_SIZE_MB=5

# Frontend origin allowed for CORS (development)
FRONTEND_ORIGIN=http://localhost:3000